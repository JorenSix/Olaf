# Testing Olaf with Zig

Run all tests with:

```bash
zig build test --summary all
```

The test layout, the output snapshot and how to write new tests are described in [tests/README.md](../tests/README.md).
