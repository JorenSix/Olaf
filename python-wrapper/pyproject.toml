[project]
name = "python-wrapper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "cffi>=1.15",
    "librosa>=0.10",
    "matplotlib>=3.7",
    "numpy>=1.22",
]
